const UsersList = (props) => {
    //const name = props
    for(const item of props.items){
        console.log(item.name)
    }
    return(
        <div>
            {
            }
        </div>
    );
}

export default UsersList;